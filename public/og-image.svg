<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 1200 630">
  <!-- Background -->
  <rect width="1200" height="630" fill="#0F172A"/>
  
  <!-- Gold accent gradient -->
  <defs>
    <radialGradient id="glow" cx="50%" cy="50%" r="50%">
      <stop offset="0%" stop-color="#D4AF37" stop-opacity="0.3"/>
      <stop offset="100%" stop-color="#0F172A" stop-opacity="0"/>
    </radialGradient>
  </defs>
  <circle cx="600" cy="315" r="400" fill="url(#glow)"/>
  
  <!-- Logo text -->
  <text x="600" y="280" font-family="Georgia, serif" font-size="72" font-weight="bold" fill="#FFFFFF" text-anchor="middle">OpenClaw</text>
  
  <!-- Tagline -->
  <text x="600" y="360" font-family="system-ui, sans-serif" font-size="32" fill="#D4AF37" text-anchor="middle">Your Own AI Chief of Staff</text>
  
  <!-- Subtext -->
  <text x="600" y="420" font-family="system-ui, sans-serif" font-size="24" fill="#94A3B8" text-anchor="middle">Set Up For You</text>
</svg>
